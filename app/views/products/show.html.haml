#main
  %h3 #{@product.name}
  %h2 Listing Expires: #{@product.end.to_formatted_s}
  %h2 Time left: 
  %table.products_table
    %tr.products_header
      %th Pledges Made So Far
      %th Target Number of Pledges
      %th Price
    %tr.product_info
      %td= @product.pledges
      %td= @product.target
      %td= number_to_currency(@product.price)

/* put the form to enter pledges here */

  %h2 Product Description
  %h1= @product.description

